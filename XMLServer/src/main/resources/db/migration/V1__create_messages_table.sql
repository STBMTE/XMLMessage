CREATE TABLE IF NOT EXISTS messages (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    time TIME NOT NULL,
    "user" TEXT NOT NULL,
    "text" TEXT NOT NULL,
    result INT NOT NULL,
    CONSTRAINT CK_MESSAGES_RESULT CHECK (result IN (0, 1))
);
